/**
 * Funciones puras de manejo de fechas, reutilizables en toda la app.
 * Mantenerlas aparte evita repetir cálculos de fechas dentro de las entidades o servicios.
 */

export function sumarDias(fecha: Date, dias: number): Date {
  // TODO: implementar lógica
  return fecha;
}

export function diasEntre(fechaInicio: Date, fechaFin: Date): number {
  // TODO: implementar lógica
  return 0;
}

export function esAnterior(fecha: Date, referencia: Date): boolean {
  // TODO: implementar lógica
  return false;
}
